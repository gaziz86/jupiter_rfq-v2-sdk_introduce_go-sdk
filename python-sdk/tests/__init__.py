"""Test suite for the RFQv2 Python SDK.

Mirrors ``rust-sdk/tests/`` — unit tests live alongside the integration tests
in :mod:`tests.test_ultra_api_e2e`. Integration tests are gated behind the
``integration`` pytest marker and require live services + env vars (see
``tests/README.md``).
"""
